DB_CONFIG = {
    "host":     "localhost",
    "user":     "root",
    "password": "",  # change to your MySQL password
    "database": "stock_mgmt_db",
}

# Low-stock alert threshold (used as default if not set per product)
DEFAULT_MIN_STOCK = 10
